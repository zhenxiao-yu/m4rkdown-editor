# Copy to .env.local for local development

# PartyKit arena server (local: http://localhost:1999, prod: run `npx partykit deploy`)
VITE_PARTYKIT_HOST=localhost:1999

# Canonical site URL used in OG meta tags (no trailing slash)
VITE_SITE_URL=http://localhost:5173
